# UniModules 基盤コード編集ルール

このディレクトリはプロジェクト非依存の共有基盤。編集時は以下を厳守すること。
利用側プロジェクトのルート CLAUDE.md にある共通コーディング規約も全て適用される。本ファイルは基盤特有の追加ルール。

**本ファイルを含め、UniModules 配下に置くものにはプロジェクト固有の情報（利用側の名前・パス・ツール環境）を書かない。**

## コーディングスタイル【厳守】

**編集前に同ファイル・同モジュールの既存コードを読み、そのスタイル・パターンに完全に合わせる**。基盤全体の頻出パターン:

- クラス内は `//----- params -----` `//----- field -----` `//----- property -----` `//----- method -----` で区切る
- イベント公開は Subject 遅延初期化: `private Subject<T> onXxx = null;` フィールド + `OnXxxAsObservable()` で `return onXxx ?? (onXxx = new Subject<T>());`。発火側は `if (onXxx != null){ onXxx.OnNext(x); }`
- GameObject 操作は `UnityUtility` 経由（`SetActive` / `SetParent` / `SafeDelete` / `DeleteGameObject` / `IsNull` / `Instantiate` 等。Unity API を直接呼ばない）
- 購読の解除は `.AddTo(Disposable)`（`Singleton` / `LifetimeDisposable`）または `.AddTo(this)`（MonoBehaviour）
- 開発ログは `UnityConsole.Event(ConsoleEventName, ConsoleEventColor, message)`（モジュール毎のイベント名・色を使う）、本番に残すエラーは `Debug.LogError` / `Debug.LogException`。**ログ・例外メッセージは英語**
- namespace は `Modules.<モジュール名>`。新規ファイルはフォルダ名と namespace を一致させる（既存の不一致一覧は `Documents~/Overview.md` 参照。新規で不一致を増やさない）
- 大きいクラスは責務毎に partial 分割する（例: `SceneManagerBase.cs` / `SceneManagerBase.Append.cs`）
- 新規コードで `Awake` / `Start` 等の Unity ライフサイクルメソッドを使わない（明示的な `Initialize()` / `Setup()` + `private bool initialized = false;` フラグ。既存の使用箇所は指示があるまで維持）
- 汎用処理（文字列・コレクション・GameObject 操作等）を書く前に `Extensions/` の既存実装を確認する（`Documents~/Extensions/Methods.md`）

## プロジェクト非依存【厳守】

- 利用側プロジェクトの型・定数・文言・アセットパスを基盤から参照しない
- プロジェクト固有の値・処理が必要な場合は、abstract メンバー・ジェネリック型引数・イベント公開のいずれかで利用側から注入させる形にする

## 変更時の影響調査【厳守】

- 公開 API のシグネチャ・イベントの発火順・初期化/解放の順序を変える前に、**利用側の全使用箇所（呼び出し・購読）を grep で列挙**し、影響を確認してから変更する
- 既存の公開 API は互換を維持する。破壊的変更（シグネチャ変更・削除・発火タイミング変更）はユーザー（基盤管理者）の明示的な合意を得てから行う
- 編集後はコンパイル確認まで行う

## コードコメントの記述方針【厳守】

コメントは**自モジュールが保証する契約・制約のみ**を現在形で書く。

- 利用側（本基盤を利用するプロジェクト側のコード）や他モジュールの実装内容・購読内容に言及しない
  - ❌ `// 購読側でポップアップ親の削除が行われるため、それより先に発火する.`
  - ✅ `// 加算遷移通知より先に発火する.`
- 特定の利用ケースの具体名（「ポップアップ退避のため」等）を根拠として書かない（基盤は汎用であり、利用目的はコメントの根拠にしない）
- 変更経緯・修正理由を書かない（「〜の不具合対策」「今回〜に変更」等。履歴は git で追える。コメントは現在の仕様のみ）
- 他者の実装に触れたくなったら自分側の要件へ一般化する（「Xの削除より先に」→「Xの通知の購読処理より先に完了させる」）
- 書くべきコメントは「コードに現れない制約」（発火順序・初期化順・スレッド制約等）

## ドキュメント（Documents~/）

- UniModules 配下の内容のみを記載する（利用側プロジェクト固有の情報は利用側のドキュメントへ。本基盤側には書かない）
- 現在の仕様のみを現在形で書く（修正経緯・更新履歴は書かない）
- コード変更でドキュメントとズレたら該当記述を最新仕様で更新する
